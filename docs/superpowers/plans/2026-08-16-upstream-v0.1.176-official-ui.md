# Sub2API v0.1.176 Official UI Sync Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rebuild `aiwelink-dev` on the exact Sub2API `v0.1.176` application tree while retaining only AIWeLink registration, referral, authentication-safety, version, CI, and documentation changes.

**Architecture:** Keep the corrective branch based on the latest `origin/aiwelink-dev` for merge continuity. Replace only the `backend/` and `frontend/` application trees with the upstream tag, then replay known clean commits that were authored directly on the tag or port their individual hunks. Existing AIWeLink repository automation and documentation remain outside the application reset and are audited separately.

**Tech Stack:** Git worktrees and selective cherry-picks, Go 1.26.6, Vue 3/TypeScript with pnpm, Bash contract tests, GitHub Actions.

---

### Task 1: Establish the sync contract

**Files:**
- Modify: `deploy/tests/ci-workflow-contract-test.sh`
- Create: `docs/superpowers/specs/2026-08-16-upstream-v0.1.176-official-ui-design.md`
- Create: `docs/superpowers/plans/2026-08-16-upstream-v0.1.176-official-ui.md`

- [x] Add assertions for `VERSION=0.1.176-1`, `UPSTREAM_VERSION=0.1.176`, and absence of `workbench-*` application markers.
- [x] Add an optional local tag comparison that rejects frontend paths outside the auth/version allowlist.
- [x] Run `bash deploy/tests/ci-workflow-contract-test.sh` on the current tree and record the expected failure (`found 0.1.170`).

### Task 2: Restore the official application tree

**Files:**
- Replace: `backend/**` from `v0.1.176`
- Replace: `frontend/**` from `v0.1.176`

- [ ] Save the current branch as the integration base; do not touch the primary worktree.
- [ ] Run `git restore --source=v0.1.176 --staged --worktree -- backend frontend`.
- [ ] Commit the tree reset as `sync: restore official Sub2API v0.1.176 application tree`.
- [ ] Verify `AppLayout.vue`, `AppHeader.vue`, `AppSidebar.vue`, dashboard views, and `style.css` match `v0.1.176` before overlays.

### Task 3: Replay registration and referral behavior

**Files:**
- Modify or create only the growth registration files listed in the design spec, including `backend/internal/config/config.go`, server middleware/routes, auth registration wiring, repository/service wiring, and migration `194_growth_registration_outbox.sql`.
- Preserve focused growth tests and deployment configuration examples.

- [ ] Cherry-pick the tag-based commits in order: `0b7eadb3c`, `fc626e69c`, `634a52bfe`, `52ce68173`, `c7bb2f01f`, `1a536ae2d`, and `69b2bf37e`.
- [ ] Resolve conflicts by keeping the `v0.1.176` implementation and applying only growth registration hunks; never restore an old whole file over upstream code.
- [ ] Apply referral commits `d7286b2bd`, `1e3b8b7c7`, and `85c41ce12` as focused patches.
- [ ] Run the focused Go growth tests and inspect generated wiring before committing.

### Task 4: Port authentication safety and narrow security fixes

**Files:**
- Modify only auth API/client/token-refresh, pending-registration/OAuth callback files, and the corresponding tests.
- Modify only the narrow CodeQL files proven absent from `v0.1.176`.

- [ ] Apply `63614ff8d` and `446005be5` with upstream conflict resolution; use the official pending OAuth and refresh-token implementation wherever it already contains the same fix.
- [ ] Apply only the non-overlapping security patches `4484b181b`, `74eefa14e`, `f01fe8ae6`, `4b7bdd155`, and `a18075e74`.
- [ ] Confirm no workbench, dashboard, theme, Grok, channel-monitor, or pricing files enter the diff.
- [ ] Run focused frontend auth tests and focused Go security tests.

### Task 5: Port version identity and release automation

**Files:**
- Modify: `backend/cmd/server/VERSION`, `backend/cmd/server/UPSTREAM_VERSION`
- Modify: `backend/internal/versioninfo/**`, version/update service wiring, public settings DTO/handler, and the official `VersionBadge`/store files.
- Modify: `.github/workflows/release.yml` and related release contract tests only as needed.

- [ ] Port the dual-version behavior from `2125297a1` without copying old UI layout files.
- [ ] Set the metadata to `0.1.176-1` and `0.1.176`.
- [ ] Require the exact release tag `v0.1.176-1`, and validate that tags without `v` or upstream-only tags are rejected.
- [ ] Run `backend/scripts/validate-version.sh`, its positive and negative cases, and the focused version tests.

### Task 6: Audit, verify, and publish

**Files:**
- Modify only files discovered by the preceding audits.

- [ ] Run the focused Go and frontend test commands from the design spec, plus frontend typecheck/build when the local toolchain permits.
- [ ] Run `bash deploy/tests/ci-workflow-contract-test.sh` and `git diff --check`.
- [ ] Compare `git diff --name-only v0.1.176` against the explicit allowlist; fail on any console/UI overlay or broad upstream replacement.
- [ ] Commit the implementation, push `sync/upstream-v0.1.176-official-ui`, and create a ready PR targeting `aiwelink-dev` without requesting reviewers.
