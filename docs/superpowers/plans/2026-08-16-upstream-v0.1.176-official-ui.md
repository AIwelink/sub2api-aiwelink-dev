# Sub2API v0.1.176 Official UI Sync Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the reverted `0.1.170` application tree with official Sub2API `v0.1.176`, then reapply only the approved AIWeLink registration, auth safety, versioning, CI, deployment, and documentation changes.

**Architecture:** Keep the branch based on the latest `origin/aiwelink-dev` history, but create an explicit tree replacement commit from upstream tag `e803e3851`. Reapply narrow commits that were already ported to the 176 codebase, resolving overlaps in favor of upstream behavior. Enforce the UI and version boundary with a focused shell contract.

**Tech Stack:** Git, Go 1.26.6, Vue 3, TypeScript, Vitest, shell-based CI contracts, GitHub Actions.

---

### Task 1: Define The Failing Sync Contract

**Files:**
- Create: `deploy/tests/upstream-sync-contract-test.sh`
- Modify: `.github/workflows/backend-ci.yml`

- [ ] **Step 1: Add assertions for `VERSION=0.1.176-1`, `UPSTREAM_VERSION=0.1.176`, exact release tag validation, official AppLayout markers, and absence of `workbench-*` source/test markers.**
- [ ] **Step 2: Run `/bin/bash deploy/tests/upstream-sync-contract-test.sh` and confirm it fails on the current `0.1.170-1` tree.**
- [ ] **Step 3: Add the contract to the existing shell/CI contract job without changing unrelated CI behavior.**
- [ ] **Step 4: Commit the red contract.**

### Task 2: Restore The Exact Upstream Application Baseline

**Files:**
- Replace from `v0.1.176`: all tracked repository paths.
- Preserve from the sync branch: the new design, plan, and red contract files only.

- [ ] **Step 1: Restore the repository tree from `v0.1.176` while keeping the branch based on `origin/aiwelink-dev`.**
- [ ] **Step 2: Restore the Task 1 contract and planning documents after the tree replacement.**
- [ ] **Step 3: Verify `frontend/src/components/layout/AppLayout.vue` matches the official 176 blob and `workbenchVisualContract.spec.ts` is absent.**
- [ ] **Step 4: Commit the official 176 baseline replacement.**

### Task 3: Reapply Registration And Referral Only

**Files:**
- Modify/create only `backend/internal/config/growth_registration_test.go`, `backend/internal/server/middleware/growth_registration_session*`, `backend/internal/service/growth_registration*`, `backend/internal/repository/growth_registration*`, `backend/internal/server/routes/growth_referral*`, `backend/internal/web/bypass*`, migration `194_growth_registration_outbox.sql`, and the minimum existing config/router/auth/repository/Wire files.

- [ ] **Step 1: Cherry-pick the clean 176 registration series in order: `0b7eadb3c`, `fc626e69c`, `634a52bfe`, `52ce68173`, `c7bb2f01f`, `1a536ae2d`, `f35213a30`.**
- [ ] **Step 2: Port referral commits `d7286b2bd`, `1e3b8b7c7`, and `85c41ce12`, resolving conflicts by keeping official 176 code outside referral hunks.**
- [ ] **Step 3: Apply focused overflow fixes `4484b181b`, `74eefa14e`, and `f01fe8ae6`, then atomic registration commit `69b2bf37e`; omit unrelated overlay commits.**
- [ ] **Step 4: Run focused growth middleware, route, service, repository, and registration-binding tests.**
- [ ] **Step 5: Commit any conflict-resolution-only changes separately.**

### Task 4: Reapply OAuth Registration Safety Without UI Redesign

**Files:**
- Modify only auth API/store/helpers, registration/OAuth views and tests, and backend OAuth pending-session handlers/tests.

- [ ] **Step 1: Cherry-pick `63614ff8d` for in-memory refresh and pending-registration secrets.**
- [ ] **Step 2: Cherry-pick `446005be5` for server-owned OAuth affiliate handoff.**
- [ ] **Step 3: Apply `4b7bdd155` and `a18075e74` only if their focused security tests remain compatible with the clean 176 baseline.**
- [ ] **Step 4: Verify no layout, dashboard, theme, homepage, channel-monitor, Grok, pricing, or billing path changed.**
- [ ] **Step 5: Run focused OAuth/auth-secret/token-refresh tests.**

### Task 5: Restore AIWeLink Versioning And Repository Operations

**Files:**
- Restore the dual-version feature from `2125297a1` on the clean 176 tree.
- Restore selected PR #18, #25, #26, #32, and #33 infrastructure/documentation commits.
- Modify `backend/cmd/server/VERSION`, `backend/cmd/server/UPSTREAM_VERSION`, and strict tag validation.

- [ ] **Step 1: Port `2125297a1`, resolving backend/frontend conflicts in favor of official 176 plus the minimum AIWeLink version fields and badge text.**
- [ ] **Step 2: Set `VERSION` to `0.1.176-1` and `UPSTREAM_VERSION` to `0.1.176`; require exact release tag `v0.1.176-1`.**
- [ ] **Step 3: Reapply growth deployment and CI commits from PR #18/#26, then image summary commits `cae2b6e66` and `233ba9531`; do not restore the superseded standalone publish workflow.**
- [ ] **Step 4: Reapply PR #25 GitHub README/logo files and PR #33 workflow/report documentation.**
- [ ] **Step 5: Run version, deployment, CI, canary, and image-summary contract tests.**

### Task 6: Final Allowlist Audit And Pull Request

**Files:**
- Create: `docs/merge-reports/2026-08-16-v0.1.176-official-ui-rebuild.md`

- [ ] **Step 1: Compare the final frontend tree to `v0.1.176`; allow only auth safety, registration, version badge/store/types, and their tests.**
- [ ] **Step 2: Compare the final backend tree to `v0.1.176`; reject channel-monitor, Grok, pricing, billing, and other PR #29 overlay files.**
- [ ] **Step 3: Run the focused verification suite, frontend typecheck/build, `git diff --check`, and the sync contract.**
- [ ] **Step 4: Write the reconstruction report with root cause, conflict policy, retained allowlist, exclusions, and test evidence.**
- [ ] **Step 5: Request internal subagent spec and code-quality reviews; resolve critical or important findings.**
- [ ] **Step 6: Push `sync/upstream-v0.1.176-official-ui` and open a ready PR into `aiwelink-dev` without requesting any reviewer or approval.**
