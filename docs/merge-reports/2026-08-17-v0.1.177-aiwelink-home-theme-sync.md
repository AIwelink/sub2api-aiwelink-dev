# Sub2API v0.1.177 AIWeLink 首页与主题同步报告

## 1. 结论

本次同步从最新生产开发线 `origin/aiwelink-dev` 创建分支，通过一个双亲 merge 把 Sub2API 官方 `v0.1.177` 合入现有 AIWeLink 历史。后端业务、数据模型、迁移和控制台信息架构以上游为主体；AIWeLink 继续保留注册归因、referral、OAuth 安全、双版本、CI/发布、仓库首页，并选择性恢复已投产的应用首页和红/金主题。

最终版本身份为：

```text
集成基线             187045f117a144dfd26c41b07638314925089493
官方 tag             v0.1.177
官方 peeled commit   073e92d17178a1ccdb0a27017f572f10c9c7ab62
AIWeLink 生产参考    a13429cf479482d995052617249f9384a76874d2 (origin/main)
上游 merge commit    fd8e942227842413baee345fe61ec5272d25f2a1
AIWeLink VERSION      0.1.177-1
UPSTREAM_VERSION      0.1.177
发布标签              v0.1.177-1
```

`origin/main` 只用于首页和主题的语义/生产参考，没有被整体 merge，也不是本次应用基线。Git 祖先检查确认 `v0.1.177^{}` 是最终分支祖先，而 `origin/main@a13429cf` 不是最终分支祖先。

## 2. 同步方法

1. 从 `origin/aiwelink-dev@187045f11` 建立隔离分支；
2. 核验 annotated tag `v0.1.177` 的 peeled commit 为 `073e92d17`；
3. 先增加版本、首页、主题和 workbench denylist 合同；
4. 将 `v0.1.177^{}` 作为第二父提交合入，冲突按“上游业务优先、AIWeLink 契约窄范围保留”解决；
5. 在上游 `HomeView` 分支结构上恢复 AIWeLink 默认首页；
6. 在上游组件结构上融合红/金 token、语义前景色和动态图表 palette；
7. 运行聚焦合同、测试、类型检查、差异边界和两阶段代码审查。

该方法保留正常的上游 merge ancestry，后续可以直接判断某个官方 tag 是否已经合入，同时避免把旧 `main` 的 `v0.1.170-1` 应用 tree 覆盖到新版本。

## 3. 合并冲突和决策

### `.github/workflows/backend-ci.yml`

保留 AIWeLink 已有的 protected-branch gate、growth contract、镜像发布摘要和私有 registry 行为。Go 工具链继续从 `backend/go.mod` 的 `go-version-file` 读取，不固化容易漂移的字面版本；因此上游更新 `go.mod` 后，CI 会自动使用对应工具链。

### `.github/workflows/security-scan.yml`

保留 AIWeLink repository/secret scan 和安全门禁结构，Go setup 同样以 `backend/go.mod` 为唯一版本来源。没有把上游较窄的工作流整侧覆盖到现有安全合同上。

### `backend/cmd/server/VERSION`

上游版本只表示 Sub2API 版本，不能直接覆盖 AIWeLink 发布身份。冲突结果设置为 `0.1.177-1`；独立的 `UPSTREAM_VERSION` 更新为 `0.1.177`。

其他后端文件没有用旧 AIWeLink blob 覆盖上游实现。合并直接吸收了 v0.1.177 的 group usage rollup/timezone、OpenAI Codex turn-state、compaction/scheduler、usage cleanup 和相关迁移/测试。

## 4. 保留的 AIWeLink 能力

### 后端与认证

- growth registration session、加密 outbox 和异步投递；
- 邮件/OAuth 注册归因及 pending secret、refresh 协调和 stale-session 安全修复；
- `GET /r/:code` 邀请码校验、固定 HTTPS 跳转、主页 fallback、`no-store` 和 SPA bypass；
- 现有部署样例、推荐绑定合同和窄范围代理凭据脱敏。

上游已经提供的业务能力直接采用官方实现，不在 AIWeLink 层新建平行逻辑。

### 版本、CI 和仓库首页

- AIWeLink/Sub2API 双版本展示和更新检查；
- 精确 tag 校验、tag-to-SHA 不可变性、release/image contract；
- AIWeLink 私有 registry 分支镜像与发布摘要；
- PR #25 的 README、Logo 和 GitHub 仓库首页内容。

## 5. 应用首页恢复范围

提交 `047f76a8c` 恢复以下窄范围内容：

- `frontend/src/components/home/` 下九个首页组件；
- `useHomepageIntro` 及其生命周期测试；
- 中英文 `home.redesign` 文案；
- `HomeView` 默认分支渲染 `AIWeLinkHome`；
- 首页组件、粒子效果、intro 和分支选择测试。

上游首页优先级没有改变：管理员 `home_content` 的 URL/HTML 分支优先，compact 首页其次，只有默认分支进入 AIWeLink 首页。`HomeView` 继续从 v0.1.177 store 读取设置并执行原有认证检查。

最终审查发现最初的 `HomeView` blob 与旧 `origin/main` 完全相同。提交 `c5ed76f8c` 因此重新以上游 v0.1.177 文件为基础，只替换默认分支并删除随官方默认页一起失效的 helper/style。custom content 和 compact 分支的结构、行为与 class 均保持上游；提交 `c905932a5` 只把 compact CTA 的 `text-white` 换为 `text-on-primary`，避免深色金色主按钮的前景对比度回归。

## 6. 主题恢复范围

提交 `0bd51885a` 在 v0.1.177 组件上恢复主题语义：

- 浅色主色 `#D21F4B`，运行时 token 为 `--color-primary-500: 210 31 75`；
- 深色主色 `#FFC247`，运行时 token 为 `--color-primary-500: 255 194 71`；
- 中性黑 dark surfaces、语义文字/边框/状态前景色；
- `theme.css`、Tailwind 映射、全局效果、onboarding 和公共页面层次；
- `useThemePalette` 监听 `<html class="dark">` 并实时更新图表 palette；
- 账号弹窗、认证、支付、设置、管理、运维图表等仍需要的语义色消费者。

`AppLayout` 和 `DashboardView` 只修改颜色 class/token，没有复制旧模板或改变 sidebar 宽度、header 高度、shell offset、路由、数据区或卡片结构。`HomeView` 的非默认分支只保留上述 compact CTA 前景色 hunk。合同最初错误地按十六进制文本搜索 RGB 通道 token；提交 `a79a1f580` 将断言修正为真实运行时声明，没有修改主题颜色。

## 7. 明确排除项

- PR #21 和 `feature/workbench-ui-iteration` 的 workbench/test console 结构；
- `workbench-shell`、`workbench-content`、`workbench-header`、`workbench-sidebar` marker；
- `workbenchVisualContract.spec.ts`；
- 旧 dashboard data bands、列表/表格重排和 shell 尺寸修改；
- 从 `origin/main` 整体 merge、整文件覆盖共享上游组件或恢复旧应用 tree；
- 与首页/主题无关的旧控制台测试改动。

最终 `git grep` 对四个 workbench marker 无输出，官方 `bg-mesh-gradient` 和 `lg:ml-64` 布局 marker 仍由同步合同强制检查。

## 8. 版本命名和自动化

- `backend/cmd/server/VERSION`：`0.1.177-1`；
- `backend/cmd/server/UPSTREAM_VERSION`：`0.1.177`；
- 唯一有效发布标签：`v0.1.177-1`；
- `0.1.177-1`、`refs/tags/v0.1.177-1`、`v0.1.177`、`v0.1.177-2` 均由 validator 拒绝；
- 同步合同同时检查双版本、官方布局、默认首页、红/金 token、主题入口和 workbench denylist。

后续上游版本只需更新 `UPSTREAM_VERSION` 和 AIWeLink patch 版本，并以 `go.mod`/lockfile 作为工具链和依赖来源，不在 workflow 复制版本常量。

## 9. 聚焦验证证据

本地按项目要求执行聚焦测试，不宣称完成全仓库、容器或多平台矩阵。

- `bash deploy/tests/upstream-sync-contract-test.sh`：通过，输出 `AIWeLink 0.1.177-1 (Sub2API 0.1.177)`；
- `bash deploy/tests/ci-workflow-contract-test.sh`：通过；
- `bash deploy/tests/image-publish-summary-test.sh`：通过；
- 首页与主题最终集合：12 个 Vitest 文件、59 项测试通过；
- 主题消费者/图表/设置集合：10 个 Vitest 文件、61 项测试通过；
- `corepack pnpm run typecheck`：通过；
- `go test ./internal/server/routes ./internal/web ./internal/versioninfo`：通过，其中前两个包执行测试，未加 `unit` tag 时 versioninfo 没有测试文件；
- `go test -tags=unit ./internal/versioninfo -count=1`：通过；
- OpenAI turn-state 和 group usage 的 service 聚焦测试：通过；
- group usage repository 与 migration 的 `unit` 聚焦测试：通过；
- `v0.1.177^{}` 祖先检查：通过；
- `git diff --check origin/aiwelink-dev...HEAD`：通过；
- workbench marker 搜索：无输出。

本机存在 `D:\app\data\config.yaml`，完整 config 包的默认值测试会读取该宿主配置，因此没有把完整 `go test ./internal/config` 计入通过项。本次新增逻辑、完整 Go/Vitest/build 和跨平台组合继续由 GitHub Actions 的隔离环境执行。

## 10. 后续同步流程

1. fetch `origin` 和官方 `upstream`，记录最新 `origin/aiwelink-dev`、tag object 和 peeled commit；
2. 从最新 `origin/aiwelink-dev` 新建同步分支，不从 `main` 或旧 sync 分支继续；
3. 将精确官方 tag 作为 merge parent 合入；业务冲突优先上游，只补仍必要的 AIWeLink hunk；
4. 更新 `VERSION`、`UPSTREAM_VERSION`、精确 tag 合同和报告；
5. 首页/主题只从当前生产 `origin/main` 逐 hunk 参考，禁止整体 merge 或覆盖共享文件；
6. 执行 sync/CI/image 合同、受影响 Go/Vitest、typecheck、祖先检查、allowlist 和 workbench denylist；
7. 完成规格审查和代码质量审查后，创建面向 `aiwelink-dev` 的 ready PR；不指定 reviewer、不请求 approval、不自动合并。

## 11. 回滚方式

不要整体回退上游 merge，也不要恢复旧 `main` tree。按问题类别建立窄范围 revert/fix PR：

- 上游业务回归：针对具体上游文件/commit 修复；
- 首页问题：只回退 `047f76a8c` 或其后续首页修复；
- 主题问题：只回退 `0bd51885a` 和相关主题修复；
- 注册、OAuth、referral、版本或 CI 问题：分别按对应 allowlist 回滚。

每种回滚都必须重新运行相关聚焦测试和同步合同，确保不会重新带回 workbench 结构或降级到旧应用基线。
